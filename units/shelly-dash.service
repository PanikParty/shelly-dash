[Unit]
Description=Shelly climate dashboard webapp (FastAPI + UI, port 8088)
After=network.target

[Service]
ExecStart=/opt/shelly-dash/.venv/bin/uvicorn server:app --host 0.0.0.0 --port 8088 --app-dir /opt/shelly-dash
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
